import { Outlet } from "react-router";

const LayoutWithBarAgent = () => {

    return (
        <div>
            <h1>Hello</h1>
            <Outlet/>

        </div>
    );
};

export default LayoutWithBarAgent;
